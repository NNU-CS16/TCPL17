/*This is the third-2 question*/
#include <stdio.h>
#include <math.h>
int main()
{
  int y,x,a,b,c;
  printf("x=  a=  b=  c= \n");
  scanf("%d%d%d%d",&x,&a,&b,&c);
  y=sqrt(fabs(x));
  if(y=(4*a)/(b*c))
    printf("相等");
  else
    printf("不等");

  return 0;
}
