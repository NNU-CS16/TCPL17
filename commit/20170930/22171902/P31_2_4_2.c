#include<stdio.h>
#include<stdlib.h>
#include<math.h>
int main ()
{
	float x,y,z,a=1,b=1,c=1;
	printf("input the value of x:");
	scanf("%f",&x);
	y=sqrt(fabs(x));
	z=(4*a)/(b*c);
	if(y=z)
   {
	printf("true");
   }
	else
   {
	printf("flause");
   }
	return 0;
}
