#include<math.h>
int main()
{
  int x,a,b,c;                            //将x,a,b,c定义为int型的变量
  printf("x=,a=,b=,c=");                  //输出
  scanf("%ld,%ld,%ld,%ld\n",&x,&a,&b,&c); //格式化输入
  if(sqrt(fabs(x))!=(4*a)/(b*c))          //判断不等式是否成立
  printf("!=");
  else;
}
 
