#include <stdio.h>
#include <math.h>
int main()
{
    	
    float x,a,b,c;
    scanf("%e,%e,%e,%e",&x,&a,&b,&c);
    int d = fabs(x);
    int e = sqrt(d);
    if (e=(4*a)/(b*c))
    printf("不相等");
    else printf("相等");
			  
    return 0;
}
