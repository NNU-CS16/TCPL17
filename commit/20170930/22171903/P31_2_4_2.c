#include <stdio.h>
#include <math.h>
int main()
{
    int a=1,b=1,c=1,x;

    printf("Please input x: ");
    scanf("%d",&x);
    
    if
        (sqrt( fabs(x))!=(4*a)/(b*c))
        printf("true\n");
    else
        printf("fauls\n");

    return 0;
}
