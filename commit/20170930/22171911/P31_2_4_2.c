#include <stdio.h>
#include <math.h>
int main( )
{
 int x,a,b,c,y;
 scanf("%d%d%d%d",&x,&a,&b,&c);
if (y==(sqrt(abs(x))), y!=4*a/b*c)
  printf("%d",1);
if (y==(sqrt(abs(x))),y==4*a/b*c)
 printf("%d",0);
  
  return 0;
}
