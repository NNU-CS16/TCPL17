#include<stdio.h>
#include<math.h>
int main()
{
    int x,a,b,c;
    x=4,a=2,b=2,c=2;
     (sqrt(abs(x)))!=(4*a)/(b*c);
     if
    printf("unequal\n");
    else
    printf("equal\n");
    return 0;
}
