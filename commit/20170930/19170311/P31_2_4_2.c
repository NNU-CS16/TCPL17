#include<stdio.h>
#include<math.h>
int main()
{
	float x,a,b,c,y;
	
	scanf("%fl,%fl,%fl,%fl",&x,&a,&b,&c);

	x=fabs(x);
	 x=sqrt(x);
	y==4*a/(b*c);
	
	if (x!=y){
	printf("inequality");
	} else {
	printf("equality");
}
	
	return 0;
}
