#include <stdio.h>
#include <math.h>
int main()
{
   float x,a,b,c;
   int abs(int x);
   printf("Please input x,a,b,c:");
   scanf("%f%f%f%f,&x,&a,&b,&c");
  
   if (sqrt(abs(x))!=(4*a)/(b*c))
     printf("result=1\n");
   else
     printf("result=0\n");
   

   return 0;
}

