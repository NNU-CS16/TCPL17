#include<stdio.h>
int main()
{
	char a;
	int b;
	float c;
	double d;

	 printf("char  size=%ld\n",sizeof(a));
	 printf("int   size=%ld\n",sizeof(b));
	 printf("float size=%ld\n",sizeof(c));
	 printf("double size=%ld\n",sizeof(d));
	  
	 return 0;
}

