#include<stdio.h>
#include<math.h>
int main()
{
    float x,a,b,c;
    scanf("%d",&a);
    scanf("%d",&b);
    scanf("%d",&c);
    x=abs(x);                
    sqrt(x)!=(4*a)/(b*c);   //用编程表示该不等式
    printf("x=%d\n",x);     
    return 0;
}
