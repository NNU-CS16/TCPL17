1. 1
!(a+b)中a+b=5,5不等于0为真，进行非运算后为假，即!(a+b)=0,!(a+b)-c+1||c+b/2即为
-3||11/2,由于两者都不为0，所以进行或运算后结果为真，即1
2. c=0,d=1
c=(a&b)&&a中a与b先进行位与运算即(a&b)=0,0与1进行与运算，结果为0;同理d=1
3. D
*p++中,由于++的优先级高于*的优先级,所以先运算p++,再运算*p,结果不能使*p的值增加1.4. 9
执行f(f(2)),先运算f(2),return((x>2)?x*f(x-1):3)中,由于2不大于2,所以f(2)的值为3,所以再计算f(3),3大于2,所以执行x*f(x-1),结果为3*3==9
5. 
6. 


