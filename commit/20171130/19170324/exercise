1、1    （a+b等于非0,进行非运算，返回0。||左边值为非0，不进行右边的运算，返回1）
2、c=0  （a与b化成二进制进行位运算为0，与a=1进行&&，为0）
   d=1  （a&&b，都为非0,值为1,1&1为1）
3、c和d （c的前加加在所有运算符之前计算，指针先向后指，然后再取指向地址的元素。d          中*的优先级低于后++，所以先后++，再*)
4、9    （f（2）= 3， f（3）= 3 ）
5、d    （前面的才是定义的数据类型，后面的是名字）
6、Beijing （p+10指向第二个字符串中的i，所以输出第二个字符串）
7、d
8、d     （T就代表了定义char类型数组）
9、14
10、A    （只定义了255个元素，循环却给数组赋值了256次）
