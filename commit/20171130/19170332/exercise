1、1
    ！5=0，然后分别计算两边的值，而||的运算级最低，所以最后计算，所以因为两边都>是真，所以结果为真即为1.
2、c=0,d=1
    a是01，b是10，则a&b=0，所以c为0.a&&b为1则d为1。
3、D
    *p++是对之地址进行加的，而不是其数字
4、9
    当x为2时，f(2)=3,f(3)=9
5、C
    输入的格式不一样了，pf是float型，而f2是char 型的
6、beijing
7、D
    按值传递，函数返回后，局部变量的值不会随着调用函数内部的实现改变而改变。按地
址传递和按指针传递，才会改变局部变量的值。这道题，GetMemory（）是按照按值传递的
8、B
    此时的a为二维数组的指针变量
9、14
10、A

