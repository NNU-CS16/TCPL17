1、1
将a+b算出后进行非运算，再与后面的进行或运算。
2、c=0;d=1
a&b=0,a&&b=1,(a&b)&&a=0,(a&&b)&a=1
3、D
++优先级高于*，所以*p++指的是下一个变量的值
4、9
f(2)=3,f(3)=f(2)*3=9
5、C
pf指向的函数参数类型和f2函数的参数类型不一样
6、Beijing
定义了字符阵列是10个字符，未满的自动补空位，所以p+10指向的第一个字符是B，所以输出的是Beijing。
7、D
在函数中给指针分配空间，实际上是给指针的临时变量分配空间，函数结束后，这个临时变量也消亡，而str仍然为NULL，没有为其分配空间，此时strcpy()是肯定会出错的。
8、D
9、14
将65530转化成二进制
10、B
unsigned char 的取值范围是0-255，当超出取值范围的时候，程序自动舍弃最高位，变成了0，程序就如死循环。
