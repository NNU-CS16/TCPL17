一、 1 （true      !5 为 0 ， -2 || 5 为真）;
二、 c = 0, d = 1; (0 && 1 == 0; 1 & 1 == 1);
三、 D (* ++ 优先级相同 即为 *（p++）p指向year 下一个地址的值);
四、 9;
五、 C（函数地址的获取，可以是函数名，也可以在函数名前加取地址符 & ）; 
六、 Beijing ;
七、D(在函数中给指针分配空间， 实际上是给指针的临时变量分配空间，函数结束后这个临时变量随之消	失，因此str仍为NULL，所以strcpy函数会出错);
八、B;
九、14;
十、A B(1、数组下标从0开始，2、unsigned char 取值范围在（0, 255）i在为255,i++ 后又为0 ，死循环);
