1:1
由逻辑运算符运算规则得到

2：c = 1; d = 0;

3:D
A. *p += 1:通过指针将指针所指向的变量的值加1
B. (*p)++;:先去指针p指向的值，再将该值自增1
C. ++(*p):先去指针p指向的值,再将该值自增1
D. *p++:先取指针p指向的值，再将指针p自增1；
	所以选D

4:9
(表达式)? x : y ;
首先计算表达式的值，若为ture，则"(表达式)? x : y "返回x；否则返回y
所以结果为3*3=9

5：C

6:Beijing
指针p+10指向[10]内的东西，直到遇到一个"\0",所以输出Beijing

7：D
虽然在函数内部分配了内存空间并把地址赋予指针p，但是函数调用结束时，系统会自动销毁形参指针*p，所以函数调用后并没有返回分配好的内存地址给指针str，str是没有内存空间的，调用strcpy函数往一个空的内存空间写内容，会导致程序崩溃，而且函数内申请的堆空间没有手动释放，容易造成内存泄露。

8：D

9：14

10：B
