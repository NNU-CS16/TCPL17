#include <stdio.h>

int is_pow2(int n);
int main()
{
  int n; 
  scanf("%d",&n);
  int is_pow2(int n);
  return 0;
}
int is_pow2(int n)
{
  if(((n&n-1)==0)&&n>0) 
  return 0;
  else
  return 1;
}

    
 
