#include <stdio.h>
int n, k, a, b;  
int arr[1001];  
int main()
{  
    scanf("%d%d", &n, &k);  
    arr[0] = 1; 
{
    if (n == 1)
	return a;
    if (n != 1 && n < k)
	return (n - 1, n ,b % 100007, (b + n) % 100007);
    else
	return (n - 1, k, b % 100007, (a + b) % 100007);
}
    printf("%d\n", arr[n]);  
    return 0;
}  
