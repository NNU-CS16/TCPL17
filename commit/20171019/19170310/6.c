#include<stdio.h>
int main ()
{
 int a,b,c;
 scanf("%d%d%d",&a,&b,&c);
 if(a==b)
 printf("Unlike other ball weights, the c ball");
 else
 {
  if(a==c)
  printf("Unlike other ball weights, the b ball");
  else
  {
   printf("Unlike other ball weights, the a ball");
  }
 }
return 0;
}


