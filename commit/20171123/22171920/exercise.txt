1.
	a=2,b=0,c=1,d=2

	++a等于2，d=2*1=2

2.
	15

	SUB(a++,++b)先将a=3代入运算，再将4赋值给a，++b等于5，所以SUB(3,5)=3*5=15	

3.
	i>j

	因为int转化为unsigned int

4.
	2,5

5.
	10,20,30

	p[0][0]=10,*(p[0]+1)=20,(*p)[2]=30	

6.
	0

	*(p+4) -> '\0' -> 0

7.
	2

	(m = a > b) = 0,则不运算其后的式子，即n仍为2

8.
	10,4

	sizeof返回数组所占的字节数，'wang' 'miao'共占8字节，显式'\0'占1字节，字符串末尾隐式'\0'占1字节，共10字节。strlen返回字符串的长度，以遇到'\0'结束符为准，因此为4
	
9.
	0x801005,0x801014

10.
	7

	静态变量只会被初始化一次,即n=1+1=2,i=2;n=2+2,i=3;n=4+3=7,i=4
