1.1,o,1,2
c赋值为1,++a是2
2.15
3×5=15
3.i>j
int转化成unsigned int
4.2,5
*(a+1)=a[1]=2,&a为取数组名的地址，即为整个数组的地址，它的类型为数组类型，(int*)(&a+1)将其强制转换成整型指针赋值给*ptr，并指向了数组界外,最后用*(ptr-1)又指向了界内的最后一个元素5。
5.10,20,30
p[0][0]=10,*(p[0]+1)=arr[0][1]=20,(*p)[2]=arr[0][2]=30
6.0
*(p+4) -> '\0' -> 0
7.2
前面不符合后面n不变
8.10,4
sizeof返回数组所占的字节数，'wang' 'miao'共占8字节，显式'\0'占1字节，字符串末尾隐式'\0'占1字节，共10字节。strlen返回字符串的长度，以遇到'\0'结束符为准，因此为4。
9. 0x801005, 0x810028
10.7
n = 1, i = 1;
n = 2, i = 2;
n = 4, i = 3;
n = 7, i = 4 
