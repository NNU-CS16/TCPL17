#include <stdio.h>
int main()
{
    #defin FNX(x) (int)(xo+(x)*1.0);
    #defin FNY(y) (int)(getmaxy()-(yo+(y)*1.0));
    #defin FNX2(phi) cos(phi)*ac-sin(phi)*bs;
    #defin FNY2(phi) cos(phi)*as+sin(phi)*bc;
    return 0;
}
