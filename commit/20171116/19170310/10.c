#include<stdio.h>
#include<string.h>
int my_strlen(const char* str);
int main()
{
    char arr[100];
    int i= 0;
    printf("input string\n");
    scanf("%s/n",arr[100]);
    my_strlen(const char* str);
    printf("你输入的字符串的长度为%d\n",i);
    return 0;
}
int my_strlen(const char* str)
{
    gets(arr);
    while(a[i] != '\0')
        i++;
}
